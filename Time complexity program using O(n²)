#include <stdio.h>
#include <time.h>

int main() {
    long n, i, j;
    clock_t start, end;
    double time_taken;

    printf("Enter input size (n) for O(n^2) program: ");
    scanf("%ld", &n);

    start = clock();

    for(i = 0; i < n; i++) {
        for(j = 0; j < n; j++) {
            long y = i + j;
        }
    }

    end = clock();
    time_taken = ((double)(end - start)) / CLOCKS_PER_SEC;

    printf("O(n^2) operation executed in %f seconds\n", time_taken);

    return 0;
}
